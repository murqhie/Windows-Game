/**
 * Package of the model classes.
 * <p>
 * The model contains the game logic.
 * The model should never have a reference to the view object.
 * Instead, it should communicate the game states to the view through the controller.
 */
package Models;